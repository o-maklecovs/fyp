<!doctype html>
<html lang="en">
    <head>
        <title>{{title}}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <header>
            <h1><a href="/" class="nav-title">myJobs</a></h1>
            <div id="menu-toggler">
                <div></div>
                <div></div>
                <div></div>
            </div>
        </header>
        <nav>
            {{#if is_logged_in}}
            <div>
                {{#if is_employer}}
                <a href="/profile-employer">Profile</a>
                {{else}}
                <a href="/profile">Profile</a>
                {{/if}}
                <a href="/logout">Log out</a>
            </div>
            {{else}}
            <div>
                <a href="/login">Login</a>
                <a href="/employer">Employer</a>
            </div>
            {{/if}}
        </nav>
        {{{body}}}
        <footer>&copy; Olegs Maklecovs 2022</footer>
        <script src="/javascript/nav.js"></script>
    </body>
</html>